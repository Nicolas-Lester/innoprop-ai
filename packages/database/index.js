'use strict';
// Runtime shim — TypeScript source (index.ts) is used by the compiler for types.
// Node.js uses this file at runtime after nest build.
const client = require('@prisma/client');
module.exports = client;
